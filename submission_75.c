void 42topic_2021{
int life=100;

while(--life){
  code();
  while(is_segf()) fix();
  int result = review();
  if (result == cheat) break;
  else if (result) continue;
  if (grip_strength > 11) open("crystalgeyser");
  if (paypay >= 135,000) buy("Powertec");
  if (atcoder_rate >= 1600) cout << "GJ" << endl;
  if (level >= 1) get("Note premium");
  if (level >= 3) get("42Parker");
  pthread_create(&thread1, NULL, &build_mercari, &motivation);
  pthread_create(&thread2, NULL, &AWS, &motivation);
  pthread_create(&thread3, NULL, &cyberagent, &motivation);
  pthread_join(thread1, NULL);
  pthread_join(thread2, NULL);
  pthread_join(thread3, NULL);
}
cout << "BH" << endl;
}
